#include "showbuilding_widget.h"

//ShowBuilding_widget::ShowBuilding_widget(QWidget *parent,have_file* _file) : QWidget(parent)
//{
//    if(!file) throw "FUCK YOU";
//   final_price=new QLabel(QString::fromStdString(to_string(file->total_price())));
//   building* temp=dynamic_cast<building*>(file);
//   if(temp){
//       pic=new QLabel(QString::fromStdString(temp->get_picture()));
//       address=new QLabel(QString::fromStdString(temp->get_address()));
//       total_area=new QLabel(QString::fromStdString(to_string(temp->get_total_area())));
//   }else {
//       flat* temp=dynamic_cast<flat*>(file);
//       if(temp){
//           pic=new QLabel(QString::fromStdString(temp->get_picture()));

//       }
//   }
//}
